interface:
  display_name: "Test Quality (Fallback)"
  short_description: "Fallback: Write and review contract-focused tests"
  default_prompt: "Use $test-quality-fallback to assess and improve tests within the requested scope, then report every residual verification issue and assessment limitation."
