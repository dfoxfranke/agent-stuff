interface:
  display_name: "Unsafe Code in Rust (Fallback)"
  short_description: "Fallback: Write and review justified, sound unsafe Rust"
  default_prompt: "Use $unsafe-code-in-rust-fallback to decide whether this unsafe Rust is justified and review its safety contracts and comments."
